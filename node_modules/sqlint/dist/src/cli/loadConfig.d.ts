import { Config } from '../rules';
export declare type RawConfig = {
    rules: {
        [key: string]: string | number | {
            level: string | number;
            option: any;
        };
    };
};
export declare function convertToConfig(rawConfig: RawConfig): Config;
export declare function loadConfig(directoryOrFile: string): Config;
